[project]
name = "coupang-api"
version = "0.1.0"
description = "Coupang Open API wrapper"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["coupang"]

